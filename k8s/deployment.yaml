apiVersion: apps/v1
kind: Deployment
metadata:
  name: leaseguard-backend
  labels:
    app: leaseguard-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: leaseguard-backend
  template:
    metadata:
      labels:
        app: leaseguard-backend
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: backend
        image: ghcr.io/projects-hacks/lease-guard-backend:main
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: leaseguard-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
